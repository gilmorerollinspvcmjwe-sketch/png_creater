# 项目进度

## 当前状态
全部算法增强实现完毕（共 9 项），背景色检测和智能容差已修复，构建通过。

## 刚完成的内容
- K-Means 边框聚类：替代角点采样，边框全周采样 + K=3 聚类找背景色
- 外边框检测：自动检测并排除图片四周的边框线，避免干扰背景色识别
- 开运算自适应：自动检测前景变化率，对比度低或细线多时跳过开运算
- Watershed 分水岭分割：对过大的粘连连通块自动拆分（距离变换 + 标记扩展）
- **修复 sampleBorderColors**：三步法（粗估→过滤→K-Means），先取四角众数粗估背景色，再过滤掉距离粗估值 > 25 Lab 的点，最后 K=2 K-Means 找最大簇
- **修复 otsuAutoThreshold**：不用 Otsu，改为边框采样→排序去尾→计算均值和标准差→容差 = 均值 + 2.5*标准差（覆盖约 99% 背景纹理）

## 改动文件
- `src/workers/imageProcessor.js` — 所有算法改动集中在此
- `src/utils/colorUtils.js` — Lab 色彩距离
- `index.html` — "智能容差"复选框
- `src/main.js` — autoTolerance 传参

## 关键设计决定
- K-Means 改为 K=2，RGB 距离，最远点初始化，20 次迭代
- 粗估背景色：四角内侧采样，10 级量化取众数
- 过滤阈值：Lab 距离 < 25，过滤太狠时回退到全样本
- 智能容差：边框样本距离分布，去掉最远 10% 后算均值+2.5σ
- 开运算跳过阈值：前景变化率 > 15% 时认为在吃细线
- Watershed 触发条件：区域面积 > 平均面积 3 倍且 > 5000px 且存在多个区域

## 下一步
- 用米黄纹理背景插画图测试效果
- 观察浏览器控制台日志（粗估背景色、过滤后样本数、自动容差值）
- 根据实际效果微调过滤阈值（25 Lab）和容差系数（2.5σ）
