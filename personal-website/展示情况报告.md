# 个人主页数据展示情况报告

## profile.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| phone | ❌ 未展示 | 无 | `Get-ChildItem -Path src -Filter "*.tsx" -Recurse | Select-String -Pattern "phone"` → 无结果 |
| age | ❌ 未展示 | 无 | `Get-ChildItem -Path src -Filter "*.tsx" -Recurse | Select-String -Pattern "age"` → 仅匹配到 "message" 等无关字段 |
| bio.full | ✅ 展示 | `src/pages/About.tsx:47` | `Select-String -Pattern "bio"` → `src\pages\About.tsx:47: {profileData.bio.full}` |
| jobPreference | ❌ 未展示 | 无 | `Select-String -Pattern "jobPreference"` → 无结果 |
| coreSkillsHighlight | ✅ 展示 | `src/components/home/HighlightsSection.tsx:41-42` | `Select-String -Pattern "coreSkillsHighlight"` → 有条件渲染 |
| location | ✅ 展示 | `src/components/about/ProfileCard.tsx:8,40`, `src/pages/Contact.tsx:63`, `src/components/resume/WorkExperience.tsx:32` | 多处使用 |

---

## experience.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| team | ✅ 展示 | `src/components/resume/WorkExperience.tsx:57,61` | `Select-String -Pattern "team"` → `团队 {exp.team} 人` |
| tech | ✅ 展示 | `src/components/resume/WorkExperience.tsx:50` | `Select-String -Pattern "exp\.tech"` → `{exp.tech.map((tech) => (` |
| aiModels | ❌ 未展示 | 无 | `Select-String -Pattern "aiModels"` → 无结果 |
| aiTools | ❌ 未展示（work 条目中） | 无（仅 copilot.stats.aiToolsUsed） | `Select-String -Pattern "aiTools"` → 仅匹配 copilot.stats |
| metrics | ❌ 未展示 | 无 | `Select-String -Pattern "metrics"` → 无结果 |
| certifications | ✅ 展示 | `src/components/resume/Education.tsx:7,45-52` | `Select-String -Pattern "certifications"` → 有条件渲染 |
| highlights | ✅ 展示 | `src/components/resume/WorkExperience.tsx:38-40` | `Select-String -Pattern "highlights"` → 多处使用 |
| achievements | ✅ 展示 | `src/components/resume/WorkExperience.tsx:65-70` | `Select-String -Pattern "achievements"` → 有条件渲染 |

---

## skills.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| categories[].skills[] | ✅ 展示 | `src/components/resume/SkillsRadar.tsx:87` | `Select-String -Pattern "cat\.skills"` → `{cat.skills.map((skill) => (` |
| categories[].description | ❌ 未展示 | 无 | `Select-String -Pattern "categories\.description"` → 无结果 |
| tools[].proficiency | ✅ 展示 | `src/pages/VibeCoding.tsx:82-85` | `Select-String -Pattern "\.proficiency"` → `[...Array(tool.proficiency)]` |
| tools[].duration | ❌ 未展示 | 无 | `Select-String -Pattern "\.duration"` → 无结果 |
| languages[] | ❌ 未展示 | 无 | `Select-String -Pattern "languages"` → 无结果 |
| certifications[] | ❌ 未展示（skills.json 中） | 无（experience.json 中的 certifications 已展示） | skills.json 的 certifications 字段未被引用 |
| hobbySkills[] | ❌ 未展示 | 无 | `Select-String -Pattern "hobbySkills"` → 无结果 |
| education.description | ❌ 未展示（skills.json 中） | 无 | `Select-String -Pattern "education\.description"` → 无结果 |

**注意**：skills.json 中的 `education` 字段与 experience.json 中的 `education` 不同，experience.json 的 `education[].description` 已展示。

---

## projects.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| projects[].github | ✅ 展示 | `src/components/projects/ProjectGrid.tsx:74-76`, `src/pages/ProjectDetail.tsx:105-115` | `Select-String -Pattern "github"` → 多处使用 |
| projects[].link | ✅ 展示 | `src/components/projects/ProjectGrid.tsx:79`, `src/pages/ProjectDetail.tsx:118-120` | `Select-String -Pattern "project\.link"` → 有条件渲染 |
| projects[].tech[] | ✅ 展示 | `src/components/projects/ProjectGrid.tsx:52`, `src/pages/ProjectDetail.tsx:154` 等 | `Select-String -Pattern "project\.tech"` → 多处使用 |
| projects[].highlights[] | ✅ 展示 | `src/components/projects/ProjectGrid.tsx:59-62`, `src/pages/ProjectDetail.tsx:162-166` | `Select-String -Pattern "highlights"` → 多处使用 |
| metadata | ❌ 未展示 | 仅类型定义，无渲染 | `Select-String -Pattern "metadata"` → 仅出现在类型定义中 |

---

## knowledge.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| concepts[].interviewQuestion | ✅ 展示 | `src/components/knowledge/ConceptCard.tsx:27,63-70` | `Select-String -Pattern "interviewQuestion"` → 有条件渲染 |
| concepts[].interviewAnswer | ✅ 展示 | `src/components/knowledge/ConceptCard.tsx:73-80` | `Select-String -Pattern "interviewAnswer"` → 点击展开显示 |
| concepts[].application | ✅ 展示 | `src/components/knowledge/ConceptCard.tsx:55-58` | `Select-String -Pattern "application"` → 有条件渲染 |
| concepts[].sources[] | ❌ 未展示 | 无 | `Select-String -Pattern "sources"` → 无结果 |
| concepts[].date | ❌ 未展示 | 无 | `Select-String -Pattern "concept\.date"` → 无结果 |

---

## learning.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| topics[].content | ❌ 未展示 | 无 | `Select-String -Pattern "topics\.content"` → 无结果 |
| topics[].sources[] | ❌ 未展示 | 无 | `Select-String -Pattern "sources"` → 无结果 |
| topics[].date | ❌ 未展示 | 无 | `Select-String -Pattern "topics\.date"` → 无结果 |
| topics[].summary | ❌ 未展示 | 无 | 整个 learning.json 未被任何页面引用 |

**重要发现**：learning.json 完全未被使用！没有任何页面或组件导入此文件。

---

## copilot.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| workflow | ✅ 展示 | `src/components/copilot/WorkflowDiagram.tsx:8,30` | `Select-String -Pattern "workflow"` → `{workflow[section.key]...map()` |
| multiAgentSystem.metrics | ❌ 未展示 | 无 | `Select-String -Pattern "multiAgentSystem"` → 无结果 |
| multiAgentSystem.workflow | ❌ 未展示 | 无 | multiAgentSystem 整体未被展示 |
| cases[].steps[] | ✅ 展示 | `src/components/copilot/CaseStudy.tsx:63` | `Select-String -Pattern "caseData\.steps"` → `{caseData.steps.map()` |
| cases[].deliverables[] | ❌ 未展示 | 无 | `Select-String -Pattern "deliverables"` → 无结果 |
| stats | ✅ 展示 | `src/components/copilot/EfficiencyStats.tsx:8,11-14`, `src/pages/AICopilot.tsx:43-46` | `Select-String -Pattern "stats"` → 多处使用 |

---

## vibe-coding.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| cases[].workflow[] | ❌ 未展示 | 无 | 页面使用硬编码示例，未读取 vibe-coding.json 的 cases |
| cases[].learnings[] | ❌ 未展示 | 无 | `Select-String -Pattern "learnings"` → 无结果 |
| resources[] | ❌ 未展示 | 无 | `Select-String -Pattern "resources"` → 无结果 |
| principles[].tips[] | ❌ 未展示 | 无 | `Select-String -Pattern "principles"` → 无结果 |
| tools[].features[] | ✅ 展示 | `src/pages/VibeCoding.tsx:96-98` | `Select-String -Pattern "features"` → `{tool.features.map()` |
| tools[].useCases[] | ❌ 未展示（vibe-coding.json） | 仅 copilot.json tools.useCases 展示 | vibe-coding 页面未展示 useCases |

**注意**：vibe-coding.json 的 `tools` 数据展示在 VibeCoding.tsx 中，但 `useCases` 字段未展示。`cases` 数据完全未被使用（页面使用硬编码代码示例）。

---

## prompts.json

| 字段 | 是否展示 | 前端位置 | grep 命令 |
|------|:--------:|---------|----------|
| categories[].description | ❌ 未展示 | 无 | `Select-String -Pattern "category\.description"` → 无结果 |
| templates[].prompt | ✅ 展示 | `src/components/prompts/PromptCard.tsx:21,27-29,74` | `Select-String -Pattern "template\.prompt"` → 复制/预览功能 |
| templates[].usage | ✅ 展示 | `src/components/prompts/PromptCard.tsx:62-65` | `Select-String -Pattern "template\.usage"` → 有条件渲染 |

---

## 总结

### 完全未使用的 JSON 文件
| 文件 | 状态 | 建议 |
|------|:----:|------|
| learning.json | ❌ 完全未使用 | 建议删除或创建 Learning 页面展示 |

### 未展示的重要字段
| JSON 文件 | 未展示字段 | 影响 | 建议 |
|-----------|-----------|------|------|
| profile.json | phone, age, jobPreference | 低 | 可在 Contact 页面添加 phone |
| experience.json | aiModels, aiTools, metrics | 中 | 可在 WorkExperience 组件添加 AI 模型/工具标签 |
| skills.json | categories.description, tools.duration, languages, hobbySkills | 中 | 可在 SkillsRadar 添加描述展示 |
| copilot.json | multiAgentSystem, cases.deliverables | 高 | Multi-Agent 系统是核心亮点，建议添加展示 |
| vibe-coding.json | cases, resources, principles | 高 | Vibe Coding 核心内容未展示，建议重构页面 |
| knowledge.json | sources, date | 低 | 可在 ConceptCard 添加来源链接 |

### 展示率统计
| JSON 文件 | 展示字段数 | 总字段数 | 展示率 |
|-----------|:--------:|:-------:|:------:|
| profile.json | 3 | 6 | 50% |
| experience.json | 5 | 8 | 62.5% |
| skills.json | 2 | 8 | 25% |
| projects.json | 4 | 5 | 80% |
| knowledge.json | 3 | 5 | 60% |
| learning.json | 0 | 4 | 0% |
| copilot.json | 3 | 6 | 50% |
| vibe-coding.json | 1 | 6 | 16.7% |
| prompts.json | 2 | 3 | 66.7% |

**整体展示率**：约 42.9%（19/44 字段）

---

## 优先级建议

### P0 - 高优先级（核心亮点未展示）
1. **copilot.json - multiAgentSystem**：Multi-Agent 协作系统是 300% 效率提升的核心，建议新增组件展示
2. **vibe-coding.json - cases**：Vibe Coding 案例是实践证明，建议重构页面使用 JSON 数据
3. **learning.json - 全部**：要么删除文件，要么创建 Learning 页面

### P1 - 中优先级（有价值信息）
1. **experience.json - aiModels, aiTools**：AI 模型和工具是 AI 产品经理核心能力
2. **skills.json - languages, hobbySkills**：语言能力和兴趣技能展示个人特质
3. **knowledge.json - sources**：知识来源增加可信度

### P2 - 低优先级（可选）
1. **profile.json - phone**：联系方式完整性
2. **projects.json - metadata**：生成时间等元信息

---

*报告生成时间：2026-04-03*
*验证方法：PowerShell Select-String grep 搜索*